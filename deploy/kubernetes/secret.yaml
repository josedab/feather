apiVersion: v1
kind: Secret
metadata:
  name: feather-secrets
  namespace: feather-system
  labels:
    app.kubernetes.io/name: feather
    app.kubernetes.io/component: secrets
type: Opaque
data:
  # Base64 encoded secrets
  # Example: echo -n 'your-value' | base64
  # Add your secrets here:
  # FEATHER_KAFKA_SASL_PASSWORD: ""
  # FEATHER_API_KEY: ""
