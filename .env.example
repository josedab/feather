# =============================================================================
# Feather Feature Store — Environment Variables
# =============================================================================
# Copy this file to .env and adjust values for your environment.
# All variables are optional; defaults (shown below) are applied automatically.
# See internal/config/config.go for the full configuration reference.

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
FEATHER_HTTP_PORT=8080
FEATHER_HTTP_READ_TIMEOUT=10s
FEATHER_HTTP_WRITE_TIMEOUT=10s
FEATHER_GRPC_PORT=50051
FEATHER_GRPC_MAX_CONCURRENT=1000

# -----------------------------------------------------------------------------
# Storage — Hot Tier (in-memory LRU cache)
# -----------------------------------------------------------------------------
FEATHER_HOT_MAX_MEMORY=4GB
FEATHER_HOT_EVICTION=lru

# -----------------------------------------------------------------------------
# Storage — Warm Tier (BadgerDB persistent storage)
# -----------------------------------------------------------------------------
FEATHER_WARM_PATH=/var/lib/feather/data
FEATHER_WARM_SYNC_INTERVAL=1s

# -----------------------------------------------------------------------------
# Storage — Historical
# -----------------------------------------------------------------------------
FEATHER_HISTORICAL_ENABLED=true
FEATHER_HISTORICAL_RETENTION=720h

# -----------------------------------------------------------------------------
# Ingestion — Kafka
# -----------------------------------------------------------------------------
FEATHER_KAFKA_ENABLED=false
FEATHER_KAFKA_BROKERS=localhost:9092
FEATHER_KAFKA_TOPIC=feature-updates
FEATHER_KAFKA_CONSUMER_GROUP=feather

# -----------------------------------------------------------------------------
# Ingestion — HTTP
# -----------------------------------------------------------------------------
FEATHER_HTTP_INGESTION_ENABLED=true
FEATHER_HTTP_INGESTION_PORT=8081

# -----------------------------------------------------------------------------
# Metrics
# -----------------------------------------------------------------------------
FEATHER_PROMETHEUS_ENABLED=true
FEATHER_PROMETHEUS_PORT=9090

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
FEATHER_LOG_LEVEL=info
FEATHER_LOG_FORMAT=json

# -----------------------------------------------------------------------------
# Tracing (OpenTelemetry)
# -----------------------------------------------------------------------------
FEATHER_TRACING_ENABLED=false
FEATHER_TRACING_ENDPOINT=localhost:4317
FEATHER_TRACING_SERVICE_NAME=feather
FEATHER_TRACING_SAMPLE_RATE=0.1
FEATHER_TRACING_INSECURE=false

# -----------------------------------------------------------------------------
# Security — TLS
# -----------------------------------------------------------------------------
FEATHER_TLS_ENABLED=false
FEATHER_TLS_CERT_FILE=
FEATHER_TLS_KEY_FILE=
FEATHER_TLS_CA_FILE=
FEATHER_TLS_MIN_VERSION=1.2
FEATHER_TLS_CLIENT_AUTH=none

# -----------------------------------------------------------------------------
# Sync (Edge / Central replication)
# -----------------------------------------------------------------------------
FEATHER_SYNC_ENABLED=false
FEATHER_SYNC_MODE=edge
FEATHER_SYNC_CENTRAL_ADDRESS=
FEATHER_SYNC_INTERVAL=5s
FEATHER_SYNC_BATCH_SIZE=1000

# -----------------------------------------------------------------------------
# Feature Catalog UI
# -----------------------------------------------------------------------------
FEATHER_UI_ENABLED=true

# -----------------------------------------------------------------------------
# dbt Integration
# -----------------------------------------------------------------------------
FEATHER_DBT_ENABLED=true
FEATHER_DBT_DEFAULT_ENTITY_TYPE=unknown
FEATHER_DBT_OWNER=
FEATHER_DBT_TEAM=
FEATHER_DBT_INCLUDE_SOURCES=false
FEATHER_DBT_INCLUDE_METRICS=false
