# Rust Basic Example

A minimal Rust example demonstrating Feather feature store operations.

## Prerequisites

- Rust 1.75+
- A running Feather server (`make run-dev`)

## Run

```bash
cargo run
```

## What it does

1. Checks server health
2. Stores feature values for an entity
3. Retrieves features by entity ID
