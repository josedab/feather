// Package sharding provides distributed sharding and replication for the
// Feather feature store, routing reads and writes through a consistent hash
// ring with configurable replication factors.
package sharding
